# تعديلات مشروع

هذا الملف لتوثيق أي تعديل على المنصة ومتابعة حالته.

## طريقة الاستخدام
- أي تعديل جديد يضاف أولًا كـ `غير منجز` باستخدام `[ ]`.
- عند الانتهاء من التعديل نحوله إلى `منجز` باستخدام `[x]`.
- نضيف تاريخًا مختصرًا لكل تعديل جديد لتسهيل المتابعة.

## قائمة التعديلات
- [x] تثبيت الاعتمادات في `composer.json`:
  - تغيير `minimum-stability` إلى `stable`.
  - استبدال `realrashid/sweet-alert` من `*` إلى نسخة محددة.
- [x] تقليل الملفات الضخمة المتتبعة داخل Git:
  - تنظيف/إخراج جزء كبير من `public/assets/vendor_components` (demos/tests/docs) من التتبع المباشر.
- [x] تحسين أمان الملفات المرفوعة:
  - نقل ملفات PDF من `public/note` إلى `storage/app` مع وصول محمي.
- [x] تحديث واجهة البناء والاعتمادات الأمامية:
  - تم تحديث حزم منخفضة المخاطر (`lodash`, `postcss`, `sass`) والتحقق بـ `npm run build`.
  - تم ترحيل بناء `app.js/app.scss` إلى Vite مع تحديث القالب إلى `@vite`.
  - ملاحظة: لا تزال بعض الحزم القديمة موجودة (مثل Vue 2/Axios 0.x) ويمكن ترقيتها لاحقًا كمرحلة مستقلة.
- [x] تحسين `.gitignore` في جذر المشروع:
  - إضافة قواعد عامة لملفات البيئة والسجلات ومخرجات البناء.
- [x] (2026-03-04) إصلاح ظهور زر إضافة القسم:
  - إظهار زر `إضافة قسم دراسي` دائمًا في صفحة الأقسام حتى عند القائمة الفارغة.
- [x] (2026-03-04) إصلاح عدم ظهور القسم في نموذج إضافة التلميذ:
  - توحيد/تقوية إبطال كاش lookup للأقسام (والكلاسات) لمنع بقاء نتائج فارغة بعد الإضافة.
  - إضافة تحقق صارم لتطابق `school_id + grade_id + classroom_id` عند إنشاء/تعديل القسم.
- [x] (2026-03-04) تقليل أخطاء 500 في صفحة ملفات النقاط:
  - حماية `NoteStudents.show` بمعالجة استثناء آمنة مع إعادة توجيه ورسالة خطأ بدل صفحة 500.
  - إضافة `update()` في `NoteStudentController` لإرجاع `405` بدل رمي خطأ "method does not exist".
  - تقوية `addnotestudent.blade.php` ضد بيانات ناقصة (`user`/الحقول المترجمة) لمنع انهيار أثناء عرض الجدول.
- [x] (2026-03-04) إصلاح نهائي لخطأ Blade في `NoteStudents`:
  - إضافة directive متوافقة لـ `@selected` داخل `AppServiceProvider` لضمان التوافق مع بيئات Laravel القديمة.
  - منع `ParseError: unexpected token "@"` في الصفحات التي تحتوي `@selected(...)`.
- [x] (2026-03-04) ضمان ظهور القسم فورًا في إضافة التلميذ:
  - إلغاء كاش `lookup.classSections` و `lookup.sectionById` في `SectionController`.
  - جلب الأقسام مباشرة من قاعدة البيانات لتفادي نتائج lookup قديمة بعد إضافة/تعديل قسم.
- [x] (2026-03-04) منع تكرار أخطاء Blade بعد السحب من Git:
  - تحويل جميع صيغ `@selected(...)` إلى صيغة متوافقة `{{ condition ? 'selected' : '' }}` داخل `resources/views`.
  - تحويل جميع صيغ `@php(...)` المختصرة إلى `@php ... @endphp` داخل `resources/views`.
  - تطبيق التعديلات على صفحات حرجة مثل `NoteStudents` و`Sections` وباقي القوالب المستخدمة.

## تعديلات مستقبلية
- [ ] (YYYY-MM-DD) اكتب وصف التعديل هنا.
